// Neutral Markdown AST.
//
// A small discriminated union that maps 1:1 onto renderable components.
// It carries no dependency on any parser: `parse.ts` converts third-party
// tokens into these types, `render.ts` turns them into component trees.
export {};
//# sourceMappingURL=ast.js.map